{
  "name": "veltra-wallet-watcher",
  "version": "1.0.0",
  "private": true,
  "description": "Poll a wallet and get alerted when it makes new trades, using the Veltra API",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "tsx --test src/watch.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
